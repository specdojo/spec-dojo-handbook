---
specdojo:
  id: prj-0001:pjr-jhsc-exec-events-granularity
  type: project
  status: draft
  rulebook: specdojo:pjr-rulebook
  part_of:
    - prj-0001:pjr-index
  item_type: todo
  item_status: in-progress
  priority: medium
  owner: ARC
  registered_at: "2026-08-31T22:37:12Z"
  due_on: "2026-09-30"
---

# PJR-JHSC exec のイベント粒度の理由を確認し規範文書へ記録する

## 1. 概要

exec のイベントはイベント単位でファイルへ分けられ、606 ファイルに達している。しかしこの粒度を選んだ理由が、文書にもコードのコメントにも残っていない。

理由が分からない状態では、将来の見直しを安全に行えない。ファイル数を減らせるのか、減らすと何が壊れるのかを判断できない。

現状の設計理由を調査し、規範文書へ記載する。

## 2. 完了条件

- イベントをイベント単位で分ける理由が規範文書へ記載されている。
- タスクごとの集約が可能かどうかの判断と、その根拠が記録されている。
- 集約する場合は移行方法が定められ、既存の履歴が失われない。
- 集約しない場合は、将来同じ検討が繰り返されないよう理由が明示されている。

## 3. 作業内容

| No  | 作業               | 担当   | 状態 | メモ                             |
| --- | ------------------ | ------ | ---- | -------------------------------- |
| 1   | 設計理由の調査     | ARC    | open | 実装、履歴、関連文書から復元する |
| 2   | 集約可否の判断     | ARC    | open | 壊れるものがあるか               |
| 3   | 規範文書への記載   | _TODO_ | open | exec-operation-guide ほか        |
| 4   | 集約する場合の移行 | _TODO_ | open | 判断次第                         |

### 3.1. 実装から読み取れる事実

ファイル名は `<タイムスタンプ>_<実行者>_<タスクID>_<種別>_<乱数>.json` の形式である。書き込みは新規作成のみで、既存ファイルへ追記しない。状態は全イベントを畳み込んで再構成する。

イベントソーシングの実装であり、追記をファイルの追加で表現している。

### 3.2. 並行性は理由にならない

1 つのタスクを複数の agent が同時に実行することは、claim による排他で起こらない。したがって同一タスクのイベントが同時に書かれることはない。

`exec run --parallel` で複数タスクを並行実行する場合も、タスクが異なればファイルも異なる。タスクごとに 1 ファイルとしても競合しない。

### 3.3. 考えられる理由

| 理由                      | 検討                                                                                              |
| ------------------------- | ------------------------------------------------------------------------------------------------- |
| worktree 統合時の競合回避 | 複数 worktree が同じファイルを追記すると merge 競合が起きる。ただしタスクごとに分ければ回避できる |
| 書き込みの原子性          | 新規作成は追記より安全だが、追記でも実用上の問題は小さい                                          |
| 実行者や種別での検索      | ファイル名に含まれるため `ls` で絞り込める。ただし内容を読めば同じことができる                    |

いずれもイベント単位である必然性を説明しない。調査で確定する必要がある。

### 3.4. register との対比

register のイベントは個票の Frontmatter に配列として保持され、`pjr-rulebook` に理由が明記されている。

> 項目ごとの配置により、イベントごとの追加ファイルを作らず、異なる項目を並行更新したときの共有ログ競合を避ける。

exec 側にも同等の記述が要る。粒度が異なること自体は問題ではないが、理由が説明されていないことが問題である。

### 3.5. 未決の論点

- 606 ファイルが運用上の負担になっているか。ディレクトリの一覧性や doc-index への影響を確認する。
- 集約する場合、既存のイベントをどう移行するか。履歴であるため内容は変更できない。
- ファイル名に含まれる実行者や種別を、集約後も検索できるようにするか。

## 4. 対応結果

-

## 5. 関連ドキュメント

- [[prj-0001:pjr-9rwg-register-events-separation]]: register のイベント配置。粒度の判断で対比する。
- [[specdojo:exec-operation-guide]]: exec の実行とイベント。記載先の候補。
- [[specdojo:pjr-rulebook]]: register 側の配置理由の記述例。
